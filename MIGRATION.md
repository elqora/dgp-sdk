# SDK conformance evidence

DGP SDK remains the backend domain authority. This record covers the SDK work affected by ratified DGP v1 shared contracts; it does not transfer handler, service, rate, charge, plan, delivery, action, or fulfillment semantics to Spec or a frontend package.

| Concern | Authority/evidence | Canonical treatment | Implementation status | Replacement evidence | Explicit approval |
| --- | --- | --- | --- | --- | --- |
| Handler service binding | `Catalog/Services/**`; Spec `HandlerService` | Preserve SDK service identity, nullable catalog rate, bounds, availability state, capabilities, and internal `ServiceMeta` helpers while serializing one opaque `meta` object. | Complete | Lossless valid fixture round trip; legacy-field, capability-key, missing-enabled and null-meta rejection tests | Reconciled Spec representation; backend semantics unchanged |
| Service identity | SDK catalog maps and Spec service rationale | Integer `101` and string `"101"` compare by canonical string key for lookup while round trips preserve the original scalar. | Complete | Existing repository/service tests and shared fixtures | Not required—preserve |
| ProductDefinition PHP binding | `Catalog/Schemas/**`; Spec ProductDefinition schema | Hydrate and serialize every canonical v1 property, including explicit `variant`, `multiple`, recursive options, expressions, utilities, effects, fallbacks, notices and opaque metadata. Reject aliases, derived fields and unknown nested properties. | Complete | Lossless fixture round trip, explicit variant/multiple round trip, five invalid Spec fixtures, and a 15-family nested mutation matrix | Clean-break representation approved; behavior preserved |
| ProductDefinition structural/reference validation | Existing SDK validator behavior; Spec generated schema | Enforce complete wire structure and basic node/effect reference integrity. Full contextual publication diagnostics remain owned by DGP Validation and are not independently redefined here. | Complete | `SpecContractConformanceTest` plus existing `DriverComplianceTest` reference cases | Validation ownership boundary approved |
| OrderSnapshot binding | `Snapshots/**`; Spec OrderSnapshot contract | Preserve complete canonical evidence, strict snake_case keys, quantity source, service maps, fallbacks, exact advisory utility evidence and opaque metadata. Advisory amounts are never authoritative backend charges. | Complete | Valid fixture round trip, camel-case invalid fixture, validator and runtime request tests | Advisory/frontend representation and backend authority split approved |
| Spec fixture drift | `dgp-spec/fixtures/manifest.json`; `scripts/check-contracts.php` | Every ratified manifest entry is mirrored or explicitly classified with a reason. Newly added unclassified fixtures fail the SDK check when the sibling Spec is present. | Complete | `composer check:contracts`; 14 mirrored binding fixtures and 8 explicit ownership classifications | Browser JavaScript execution is explicitly non-PHP; Validation result contracts remain Validation-owned |
| Backend domain suite | Existing action, charge, delivery, runtime, repository, compliance and hydrator tests | Preserve all established SDK-owned behavior while shared bindings change. | Complete for this affected conformance slice | Full `composer check`: 100+ tests, static analysis, contract drift, strict manifest validation | No backend redesign or retirement performed |

No backend domain behavior was retired. Browser JavaScript execution is non-applicable by ratified contract. The portable publication semantic suite is consumed by DGP Validation; SDK service DTOs provide its catalog evidence without duplicating the frontend-neutral publication engine or acquiring its diagnostic ownership.
